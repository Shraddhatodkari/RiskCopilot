[pytest]
pythonpath = .
testpaths = tests
addopts = -m "not integration"
markers =
    integration: requires live network access to data.sec.gov (excluded from default run; invoke explicitly with `pytest -m integration`)
